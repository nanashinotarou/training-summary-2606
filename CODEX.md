# Codex レビュー・デプロイルール (CODEX.md)

Codex は本ファイルを必ず読み込むこと。
**「レビューしてデプロイして」の一言で、本ファイルの手順を上から順に実行する。**

---

## -1. AI横断：Obsidian Hub（Cursor / Codex / ClaudeCode / Antigravity 共通）

**セッション開始時（必ず Read）**
- `C:\Users\Hi\OneDrive\ドキュメント\Obsidian Vault\ClaudeCode\INDEX.md`
- 本プロジェクト作業時: 同 Vault の `Instagramコース 2606.md`
- **`Retrospective.md`（全AI共有の失敗・教訓ログ）の「直近の教訓」**。過去の確定事項を再発させないために必ず読む（§8）。

**詳細ルール**: `C:\Users\Hi\OneDrive\ドキュメント\Obsidian Vault\ClaudeCode\SHARED_RULES.md`

---

## 0. Codexの担当フェーズ（1日の流れ Step 4）

Antigravity が HTML実装を終え、Hiroyaから実装完了報告（vol番号入り）を受け取ったら本フェーズに入る。

```
④ レビュー + デプロイ ─ Codex
   読む: 本ファイル + Today_Plan.md §④
   ↓
   機械チェック6本を全実行
   ↓
   ├─ 全項目Pass → そのままデプロイ（§3）
   └─ 1件でもFail → デプロイせず「Antigravity転送ブロック」を出力（§5）
```

**判定の唯一の根拠は Today_Plan.md §④ のチェックコマンドと合格値**。Day固有の数値（スライド枚数・動画数・cache-bust値）は毎回そこに書いてあるので、本ファイルに数値をハードコードせず必ず §④ を参照すること。

---

## 1. 対象ファイルの特定

Hiroyaの報告に含まれる **vol番号** を対象とする（例:「vol12-1.html 実装完了」→ `vol12-1.html`）。
報告に vol番号がない場合は推測せず、Hiroyaに確認すること。

---

## 2. 機械チェック手順

Today_Plan.md §④「機械チェックリスト」のコマンドを **一字一句そのまま**、プロジェクトルートで全件実行する。
代表的な6項目（実際の合格値は Today_Plan.md §④ を正とする）:

| # | 観点 | 合格基準 |
|---|---|---|
| 1 | 文字化け（` of ` / ` in ` / ` the `） | 0件 |
| 2 | スライド枚数（`dayXX_slide`） | §④記載の枚数 |
| 3 | 動画数（`class="video-item"`） | §④記載の本数 |
| 4 | カバースライド（`cover-slide`） | 存在（複数行ヒットは正常※） |
| 5 | cache-bust 値 | §④記載の ISO値と一致 |
| 6 | プレースホルダー残骸 | 0件 |

※ `cover-slide` はCSS/JS参照を含むため `Select-String` で複数行ヒットするが、実カバー画像は各タブ先頭1枚のみ。行数ではなく「存在するか」で判定する。

**実行結果は必ず Hiroya に表として報告する**（各項目の実測値と Pass/Fail）。

---

## 3. デプロイ（全項目Passのときのみ）

機械チェックが全項目Passした場合のみ、プロジェクトルートで以下を実行する。

```powershell
Set-Location "G:\マイドライブ\研修\【202606】Instagramコース"
.\deploy.ps1
```

- `deploy.ps1` が `.deploy_tmp` 同期・ASCII一時パス経由・中身だけコピーまで全部やる。**手動で wrangler を直叩きしないこと**（日本語パスでクラッシュする既知バグあり）
- assets を更新していない日は `.\deploy.ps1 -NoAssets` で高速化してよい
- デプロイ後、本番URL `https://training-summary-2606.pages.dev/volXX-1.html` を開いて200応答を確認し、Hiroyaに報告する

---

## 4. 禁則事項（最重要・Day10で事故あり）

- **チェック結果を合わせるためにコードを書き換えてはならない。** 数値が想定外でも、コード（CSSクラス名・HTML構造・cache-bust値）を一切変更しないこと
  - Day10で「`video-item` が8件ヒットする」問題を、CSSクラスを `vid-accordion` に改名してカウントを誤魔化した事故あり。**チェックコマンド側の解釈で対応し、コードは触らない**
- **cache-bust 値を勝手に変更しない**（Today_Plan.md 記載値が正）
- **HTML / CSS / JS を編集しない。** 修正が必要なら自分で直さず、§5 の転送ブロックを出力して Antigravity に回す
- `.git/config` に `worktreeConfig = true` を書き込まない（Antigravity無反応バグ）

---

## 5. Antigravity転送ブロック（Failのとき出力）

チェックが1件でもFailした、または目視で問題を見つけた場合、**デプロイせず**に以下の形式のブロックを出力する。
Hiroyaがこれをそのまま Antigravity に貼り付けて修正依頼する。**自己完結（vol番号・該当箇所・期待状態を明記）であること。**

```markdown
## Antigravity転送 ── vol XX-1.html 修正依頼

**対象ファイル**: volXX-1.html

**検出された問題**:
1. [問題の要約] ／ 該当箇所: [タブ名・行・該当文字列] ／ 期待: [あるべき状態]
2. ...

**修正後の必須アクション**:
- Today_Plan.md §④ のチェックコマンドを全件再実行し、結果を表で報告すること
- コードでチェック数値を誤魔化さないこと（GEMINI.md §4-7）
```

転送ブロックを出したら、Codexの作業はそこで止める（修正はAntigravityの担当。再報告が来たら §2 からやり直し）。

---

## 6. Obsidian更新（デプロイ完了後のみ）

デプロイまで完了したら以下を更新する。

- `Instagramコース 2606.md` の実装済みテーブルに当日分を追記（または既存Day行を更新）
- `INDEX.md` は **既存行を上書き更新** すること。**新規行を追加しない。** テーブルは4列（プロジェクト｜詳細ファイル｜最終更新｜一言概要）を維持する
  - Day10で重複行＋3列挿入の事故あり。必ず既存の「Instagramコース 2606」行を書き換える

---

## 7. Usage_Log 運用ルール（エージェント間トークン最適化）

**セッション開始時（必ず Read）**
- `Usage_Log.md` を読み、直近5件の全エージェントの使用量を確認する
- Antigravity の直近平均が 40% 超なら、今日のレビューで軽微な修正は自分で吸収する（Antigravityに差し戻さず、転送ブロックを出さずに対処できる範囲を広げる）
- 自分の直近平均が低い場合 → 積極的にサポートを申し出る

**タスク完了後（必ず追記）**
以下フォーマットで `Usage_Log.md` のログ末尾に1行追記する：

```
| YYYY-MM-DD | XX | review / deploy | Codex | ~X% | XX分 | Pass件数・Fail件数など |
```

- デプロイが不要だった日（Fail → Antigravity差し戻し）も記録する（コスト実態を把握するため）
- パターンが見えたら「傾向メモ」に追記する

---

## 8. 振り返り・教訓の書き先

- **`Retrospective.md`（全AI共有）— デプロイ到達ごとに必ず追記する**。Codexはデプロイ実行者なので、**デプロイ成功＝振り返りトリガー**。その日のDayエントリを起票し、自分の担当（review/deploy）で何が起きたかを1行書く（Pass/Fail件数・つまずき・教訓）。⚠️ 2026-06-28 Hiroya指示で「Hiroya指示待ち」から「必須・自動」に変更（指示待ちだから失敗が蓄積しなかった）。
- Today_Plan.md には書かない（翌日上書きで消える）
- レビューで気づいた恒久的な禁止事項は CODEX.md / GEMINI.md の該当ルールに1行昇格。横断的な重大教訓の Obsidian `Web教材制作ハンドブック` への追記は従来どおり Hiroya 指示時のみ。
