# Antigravity 実装ルール (GEMINI.md)

Antigravity は本ファイルを必ず読み込むこと。

## -1. AI横断：Obsidian Hub（Cursor / Codex / ClaudeCode / Antigravity 共通）

**セッション開始時（必ず Read）**
- `C:\Users\Hi\OneDrive\ドキュメント\Obsidian Vault\ClaudeCode\INDEX.md`
- 本プロジェクト作業時: 同 Vault の `Instagramコース 2606.md`

**作業完了時（必ず更新）**
- 上記プロジェクトファイル（実装済み・次タスク・既知課題）
- INDEX.md の「最近の重要変更」に1行追記

**詳細ルール**: `C:\Users\Hi\OneDrive\ドキュメント\Obsidian Vault\ClaudeCode\SHARED_RULES.md`

---

## 0. 必読：プロジェクト背景コンテキスト

**`CONTEXT.md`** を必ず読むこと。研修事業の背景・目的・ユーザー（Hiroya）のポジション・教材の設計思想・全体運用フローが記載されている。

---

## 1. Antigravityの担当フェーズ（1日の流れ Step 1）

Hiroyaから「今日のコース情報」を受け取ったら、以下を順番に実行する。
ClaudeCode・Cursorへの引き渡しまでが担当範囲。

### Step 1-1：コース情報の整形 → Course_Log.md に追記

Hiroyaから渡されるコース情報（Day番号・日付・テーマ・YouTube URL等）を以下のフォーマットに整形して `Course_Log.md` に追記する。

```markdown
## Day XX — YYYY/MM/DD (曜)
**テーマ:** [テーマ名]
**YouTube:** [URL]
**備考:** [Hiroyaからの補足があれば記載、なければ省略]
```

- 余分な改行・全角スペース・箇条書きの揺れなどを統一すること
- 既存エントリは絶対に編集しない。**末尾に追記のみ**

### Step 1-1b：Course_Log.md の備考（実習内容）を Today_Research.md に転記

Course_Log.md に当日Dayのエントリを追記したら、**その `備考` フィールドの実習内容を必ず Today_Research.md の末尾に追記する**。

```markdown
## 実習内容（主催者指定・原文そのまま）
[Course_Log.md の備考フィールドの内容をそのままコピー]
```

- 実習内容は Hiroya（研修主催者）が指定したもの。**YouTube動画の内容から独自に推測・創作しないこと**
- 「目標達成に向けて、課題を作成し、Padletに投稿しよう」という実習が出てきたら、以下の3つのURLを必ずWORKセクションのボタンに入れること：
  - ワークフロー: `https://platinumzone.co.jp/dx-biome/2606/dx_workflow_oshi.html`
  - Padletシェアボード: `https://padlet.com/platinumzonedx/dx-instagram-sfikt2pbwarlfa1x`
  - 初回チュートリアル: `https://platinumzone.co.jp/dx-biome/2606/dx_tutorial_june.html`

### Step 1-2：YouTube文字起こし → Today_Research.md に書き出し

対象動画を文字起こしし、以下の構成で `Today_Research.md` を**上書き**する。

```markdown
# Today_Research.md
> Day XX「[テーマ名]」リサーチ結果 — YYYY/MM/DD

## 動画情報
- タイトル：
- URL：
- 時間：

## 要点まとめ（ClaudeCode・Cursor向け）
### [セクション名]
- 要点1
- 要点2
...

## 教材に使えそうなキーワード・フレーズ
- 

## 注意点・補足
- 
```

- 文字起こし原文は保存しない（VTTファイルは処理後に削除する）
- 要点まとめは**ClaudeCodeが実装計画を立てやすい粒度**で書く
- 1動画あたり300〜600字程度を目安にまとめる

### Step 1-3：Today_Research.md 品質チェック（引き渡し前に必ず確認）

`Today_Research.md` を書き終えたら、ClaudeCodeに引き渡す前に以下を自己チェックすること。

| # | チェック項目 | 基準 |
|---|---|---|
| 1 | Day番号・日付・テーマが正確に記載されているか | Course_Log.md の当日エントリと一致 |
| 2 | YouTube URLが記載されているか | 文字起こし元と同じURL |
| 3 | 動画タイトルと尺（分）が記載されているか | 省略禁止 |
| 4 | 要点まとめが **3セクション以上** あるか | セクション数＝ページの章数の目安になる |
| 5 | 各セクションに **箇条書き2項目以上** あるか | 1行だけのセクションはNG |
| 6 | 「教材に使えそうなキーワード・フレーズ」に **3語以上** あるか | ClaudeCodeがページタイトル・見出しを作る素材になる |
| 7 | 「注意点・補足」が記載されているか | 特になければ「特になし」と明記（空欄禁止） |
| 8 | **リサーチタイトル（テーマ名）と実際の動画内容が一致しているか** | カリキュラム上のDay名と動画内容がズレる場合は、実際の動画内容に合わせたテーマ名に修正して記載する（Day02で「企業が運用をする目的」というDay名に対し実際はCanva動画だった問題） |

全項目OKならClaudeCodeへ引き渡す。不足があれば補完してから引き渡すこと。

### Step 1-4：NotebookLM プロンプトの生成

`Today_Research.md` の品質チェックが完了したら、**その日の授業内容に特化した NotebookLM プロンプト**を生成してHiroyaに渡す。

#### プロンプトの構成（固定部分 ＋ 変動部分）

以下のテンプレートの【変動部分】を、その日のリサーチ内容に基づいて埋めること。

```
Instagram運用を学ぶ社会人初心者向けの研修スライドです。
プレゼンターのスライド形式で、一枚ごとに重要ポイントを1〜2つに絞ってください。
明るく親しみやすいビジュアルで、シンプルで視覚的に分かりやすくしてください。

【今日の授業内容】
テーマ：[今日のDay番号とテーマ名]
実習内容：[今日の実習・ワーク内容を1文で]
重点的に図解してほしいポイント：
- [視覚化すべき操作手順・概念 1]
- [視覚化すべき操作手順・概念 2]
- [視覚化すべき操作手順・概念 3]
```

> 📌 **枚数について**：NotebookLMの「プレゼンターのスライド」は枚数を厳密に制御できない（ソース動画の情報量で自動決定。Day12は8〜10枚指定でも前半15枚／後半14枚が出力された）。よってプロンプトに枚数指定は書かない。HTMLに載せる枚数の取捨選択は ClaudeCode が行う（下記「引き渡し報告テンプレート」の運用どおり）。

#### 変動部分の埋め方ルール

| フィールド | 何を書くか |
|---|---|
| テーマ | `Day XX「[テーマ名]」` の形式で記載 |
| 実習内容 | 受講者が「手を動かしてやること」を1文で（例：「Canvaでリールを1本作る」） |
| 図解ポイント① | 動画内で最も多く言及された操作・手順 |
| 図解ポイント② | 受講者が一番つまずきやすそうな概念・ステップ |
| 図解ポイント③ | まとめ・比較・全体像として見せると効果的な内容 |

#### 引き渡し報告テンプレート

```
リサーチ完了しました。

【NotebookLM プロンプト（コピペしてください）】
---
（上記で生成したプロンプトをここに貼る）
---

【NotebookLM 操作手順】
1. NotebookLM（https://notebooklm.google.com）で新規ノートブックを作成
2. 今日の全動画URLをソースとして追加
3. 「ノートブックガイド」→「スライド」→「プレゼンターのスライド」を選択
4. 言語：日本語 / 長さ：デフォルト
5. 「説明」欄に上記プロンプトをコピペ → 生成
6. 全スライドを assets/ に 1.png, 2.png... の連番で保存（選別不要）
7. 準備ができたら ClaudeCode に引き渡し
```

- 画像がない場合でも ClaudeCode はテキストのみで実装できるため、画像は任意扱い
- スライドの選別（どれをHTMLに使うか）は ClaudeCode が行う
- **ClaudeCode が画像を見た後にプロンプトの精度評価を行い、改善点を Today_Plan.md にフィードバックする**

---

### Step 1-5 ← 旧Step1-4（参照用） NotebookLM スライド生成の補足手順

**① ノートブック作成**
- NotebookLM（https://notebooklm.google.com）で「新しいノートブック」を作成
- ソース追加 → 「YouTube」を選択 → 当日の全動画URLを1本ずつ追加（複数OK）

**② スライド生成**
- 右側の「ノートブックガイド」パネルを開く
- 「スライド」をクリック →「スライド資料をカスタマイズ」ダイアログが開く

**③ カスタマイズダイアログの設定**

| 項目 | 設定値 |
|---|---|
| タイプ | **プレゼンターのスライド**（ビジュアル重視でサイト映え◎） |
| 言語 | 日本語 |
| 長さ | デフォルト |

「作成するスライドについて説明してください」欄には、**§1-4 で生成したその日専用のプロンプトをコピペする**（プロンプトの正は §1-4 に一本化。ここに旧テンプレートは置かない）。

→「生成」をクリック

**④ 保存**
- 生成されたスライドを1枚ずつドラッグ＆ドロップ or 右クリック保存
- ファイル名は自動付与（`Master_..._Slide_N.png`）→ ClaudeCode がリネームするのでそのままでOK
- `assets/` フォルダに全枚数保存（選別不要）

---

## 2. 一時ファイルの運用ルール

- **`Today_Research.md`**: 上書き更新。新規ファイルを作らない
- **VTTファイル**: 処理完了後に必ず削除
- **スクラッチスクリプト**: `scratch/` フォルダに格納し、不要になったら削除
- **引き継ぎメモ等の別ファイル**: 作らない。`Today_Research.md` に集約する

---

## 3. 禁則事項

- `.git/config` に `worktreeConfig = true` を書き込まないこと（Antigravityが無反応になる既知バグ）
- `Course_Log.md` の既存エントリを編集・削除しないこと
- デプロイは行わないこと（ClaudeCode / Codex の担当）

---

## 4. HTML実装ルール（Day08以降 Antigravityが担当）

### 4-1. テンプレートの複製と差し替え

- 直前のDayファイル（例: vol09-1.html）を複製して vol10-1.html を作る
- 複製後、**全4タブ（今日の目標・前半・後半・まとめ）の本文・スライド・動画・実習を必ず差し替えること**
- テンプレート元のDay内容（ポイントカード・スライド番号・動画URL・セクション文言）が**1行でも残っていたら失格**（Day08で前半タブ丸ごと残存・後半スライド0枚の事故あり）

### 4-2. 文字化け禁止

- 日本語の助詞「の」「に」「を」が半角英語（` of ` / ` in ` / ` the ` 等）に化けることがある
- 実装後に必ず全文検索して0件を確認すること（Day08で5件発生）
- 英単語がいきなり本文に混入していたら文字化けを疑う

### 4-3. レイアウト禁止事項

- `layout-split` クラスに `@media(min-width:1200px)` の2カラム指定を**絶対に追加しないこと**
- スライド画像と説明文は縦1列（画像→テキスト or テキスト→画像）で統一

### 4-4. スライド・動画の必須属性

- 動画は `<details class="video-item">` アコーディオン形式、`loading="lazy"` と `title` 属性を必ず付ける
- スライド画像は `loading="lazy"` を付ける（カバースライドのみ除外: `class="slide-img cover-slide"` で `loading` 属性なし）
- カバースライドは各タブの先頭1枚のみ。2枚目以降に `cover-slide` クラスを付けない

### 4-5. cache-bust

- HTML末尾の cache-bust は必ず ISO 8601形式: `<!-- cache-bust: YYYY-MM-DDTHH:MM:SS -->`
- Today_Plan.md に記載された値をそのまま使う。勝手に変更しない

### 4-6. 次Dayリンク

- まとめタブの「▶ Day XXへ」リンクは、次Dayが未実装の場合 `href="#"` のまま残す
- 前Dayのまとめタブにある「▶ 当日へ」リンクを `href="#"` → 実URLに差し替えること（毎回必須・漏れ多発）

### 4-7. チェックコマンドへの対応

- ClaudeCodeやCodexから渡されるレビュー用チェックコマンドの結果が想定外の数値になった場合、**コード側を変更して数値を合わせることは禁止**
- 必ず「コマンド結果：X件、想定：Y件、原因の仮説：〇〇」と報告し、ClaudeCodeの判断を仰ぐこと（Day10でCSSクラス名を変更してカウントを誤魔化した事故あり）

### 4-8. Obsidian更新（実装完了後）

- `Instagramコース 2606.md` の実装済みテーブルに当日分を追記
- `INDEX.md` は**既存行を上書き更新**すること。新規行を追加しない。テーブルは4列（プロジェクト｜詳細ファイル｜最終更新｜一言概要）を維持する（Day10 Codexレビューで重複行＋3列挿入の事故あり）

### 4-9. 実装完了報告フォーマット（Codexへの引き継ぎ用）

実装が終わったら、Hiroyaに以下の形式で報告する。**Hiroyaはこれをそのまま Codex に貼り付けてレビュー＋デプロイを依頼する**ため、vol番号と自己チェック結果を必ず含めること。

```markdown
## 実装完了報告 ── volXX-1.html

**対象ファイル**: volXX-1.html（Day XX ○○）

**自己チェック結果**（Today_Plan.md §④ のコマンドを全件実行）:
| # | 観点 | 実測 | 合格値 | 判定 |
|---|---|---|---|---|
| 1 | 文字化け | 0件 | 0件 | ✅ |
| 2 | スライド枚数 | XX件 | XX件 | ✅ |
| 3 | 動画数 | X件 | X件 | ✅ |
| 4 | cover-slide | 存在 | 存在 | ✅ |
| 5 | cache-bust | YYYY-MM-DDThh:mm:ss | 同左 | ✅ |
| 6 | プレースホルダー | 0件 | 0件 | ✅ |

Obsidian（Instagramコース 2606.md / INDEX.md）更新済み。レビュー＋デプロイをお願いします。

[ANTIGRAVITY_COMPLETE]
```

> ⚠️ `[ANTIGRAVITY_COMPLETE]` は監視スクリプトが完了検知に使う文字列。必ず報告末尾に出力すること。

---

## 5. Usage_Log 運用ルール（エージェント間トークン最適化）

**セッション開始時（必ず Read）**
- `Usage_Log.md` を読み、直近5件の自分の使用量と他エージェントの使用量を確認する
- 自分の直近平均が 40% 超 → 今日のタスクを軽量化できないか Hiroya に提案する
- 他エージェントが連続で 40% 超 → 「このタスク引き受けます」と申し出る

**タスク完了後（必ず追記）**
以下フォーマットで `Usage_Log.md` のログ末尾に1行追記する：

```
| YYYY-MM-DD | XX | research / implement / fix | Antigravity | ~X% | XX分 | メモ |
```

- 推定%は正直に。重かった・軽かったの感覚をメモ欄に一言添える
- 同じタスクタイプで毎回重い場合 → 「傾向メモ」に追記し、次回から分割を提案する
- スライド枚数の上限（前後半各10枚）を超えそうなときは事前に Hiroya に確認する

---

## 6. 振り返り・教訓の書き先ルール

実装後に気づいたバグや改善点を記録するとき、**どこに書くかを間違えないこと**。

| 書き先 | 何を書くか | 性質 |
|---|---|---|
| `Today_Plan.md` | 作業指示・チェック値のみ | 翌日上書きで消える（一時） |
| `GEMINI.md §4` | 今後も同じ失敗を防ぐ禁止事項・注意点 | 恒久ルール |
| `Web教材制作ハンドブック` | 他AIも含む横断的な設計教訓（重大なもの） | 恒久・**Hiroyaの指示があれば書いてよい** |

**Today_Plan.mdに振り返りを書かないこと**（翌日上書きで消えるため、恒久記録にならない）。  
「今後また同じことが起きそう」と判断したら、GEMINI.md §4 の該当ルールに1行追記するのが正しい行動。  
Web教材制作ハンドブックへの追記は、**Hiroyaから「記録しておいて」等の指示があったとき**に行う（自己判断での追記は不可）。
